<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Studygram</title>
    <link rel="stylesheet" href="/list.css" />
    <link rel="stylesheet" href="/header.css" />
  </head>
  <body>
    <%- include('header.ejs') %> <% for (let i = 0; i < result.length; i++) { %>
    <a href="detail/<%= result[i]._id %>">
      <div class="content-box">
        <h2 class="content-title"><%= result[i].title %></h2>
        <p class="content"><%= result[i].content %></p>
        <p class="content-user"><%= result[i].user %></p>
      </div>
    </a>
    <% } %>
    <div class="page-btn">
      <a href="/list/prev/<%= result[0]._id %>"
        ><button><i class="fa-solid fa-angle-left"></i> 이전</button></a
      >
      <a href="/list/next/<%= result[result.length - 1]._id %>"
        ><button>다음 <i class="fa-solid fa-angle-right"></i></button
      ></a>
    </div>
    <script
      src="https://kit.fontawesome.com/a99ca2fd10.js"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
